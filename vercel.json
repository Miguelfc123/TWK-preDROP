{
  "buildCommand": "cd Frontend-App && npm install && npm run build",
  "outputDirectory": "Frontend-App/build",
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
